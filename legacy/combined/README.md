# Historical combined manifest

This directory preserves the pre-suite root manifest as migration evidence. It is not an installable Shibumi plugin and is excluded from every runtime profile.

The repository root intentionally has no `manifest.json`. Validate and install only the 22 child plugin roots listed in `../../contracts/plugin-suite-v1.json` through the suite adapter.
